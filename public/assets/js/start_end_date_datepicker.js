$('#start_date_view').persianDatepicker({
    observer: true,
    format: 'YYYY/MM/DD',
    altField: '#start_date'
});

$('#end_date_view').persianDatepicker({
    observer: true,
    format: 'YYYY/MM/DD',
    altField: '#end_date'
});
